import http from 'http';

class WebSocket {

  // #server: http.Server;
  // constructor(server: http.Server, options?: io.ServerOptions) {
  //   this.#server = server;
  //   this.#serverOptions = options;
  //   return this.init();
  // }

  // init(): Socket {
  //   this.socket = io(this.#server, this.#serverOptions);
  //   return this;
  // }

  // on(evt: string, callback: Function): Socket {
  //   this.socket.on(evt, callback);
  //   return this;
  // }

  // emit(name, payload): Socket {
  //   this.socket.emit(name, payload);
  //   return this;
  // }

  // send(name, payload): Socket {
  //   return this.emit(name, payload);
  // }
}

export default WebSocket;
